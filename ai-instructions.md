# AI 개발 지침

이 문서는 이 Godot 프로젝트를 수정하거나 확장할 때 따라야 하는 기본 개발 규칙을 정의한다.

## 코드 구조와 모듈화

- 코드는 가능한 한 역할별로 분리하고 재사용할 수 있도록 모듈화한다.
- 하나의 클래스는 하나의 스크립트 파일에 작성한다.
- 서로 다른 책임을 가진 클래스나 시스템을 하나의 파일에 함께 구현하지 않는다.
- 클래스는 한 가지 주요 책임만 담당하도록 설계한다.
- 공통으로 사용하는 기능은 기반 클래스, 컴포넌트 또는 별도의 유틸리티로 분리한다.
- 특정 화면이나 캐릭터에서만 동작하도록 강하게 결합된 코드를 피한다.
- 다른 클래스와의 통신에는 가능하면 Godot 시그널과 명확한 공개 메서드를 사용한다.
- 전투 규칙, 캐릭터 상태, UI 표시와 같은 서로 다른 책임은 각각 분리한다.
- 새로운 기능을 추가하기 전에 기존 클래스에서 재사용할 수 있는 부분이 있는지 먼저 확인한다.

## Scene 구성

- 화면의 대략적인 레이아웃과 노드 구조는 스크립트에서 동적으로 생성하지 말고 `.tscn` Scene 파일에 구성한다.
- UI의 주요 Control 노드, 컨테이너, 버튼, 라벨 및 배치 정보는 Scene에 저장한다.
- 스크립트는 Scene에 정의된 노드를 참조해 동작과 데이터 갱신을 담당한다.
- 런타임에 개수가 달라지는 객체처럼 동적 생성에 적합한 요소만 코드에서 생성한다.
- 재사용할 캐릭터, UI 패널, 전투 요소는 필요에 따라 독립된 Scene으로 분리한다.

## Scene 전환

- 메뉴, 전투, 결과 화면 등 일반적인 화면 전환이 필요하면 각 화면에 대응하는 새로운 `.tscn` 파일을 만든다.
- 서로 다른 화면을 하나의 Scene에서 노드를 숨기고 표시하는 방식으로 무리하게 관리하지 않는다.
- Scene마다 자신의 화면 로직을 담당하는 전용 스크립트를 연결한다.
- Scene 전환에 공통 상태나 데이터 전달이 필요하면 별도의 상태 관리자 또는 Autoload를 사용한다.

## 작업 원칙

- 기존 기능과 파일 구조를 먼저 확인한 후 변경한다.
- 새 기능은 기존 구조와 일관된 이름과 경로를 사용한다.
- 수정 후에는 Godot에서 프로젝트를 실행해 파서 오류와 런타임 오류가 없는지 확인한다.
- 구조를 변경할 때 기존 동작이 유지되는지도 함께 검증한다.
